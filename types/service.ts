export interface Service {
  id: string
  number: string
  title: string
  subtitle: string
  description: string
  features: string[]
}

export interface ProcessStep {
  number: string
  label: string
}
